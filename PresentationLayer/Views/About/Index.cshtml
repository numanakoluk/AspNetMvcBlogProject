@model IEnumerable<EntiyLayers.About>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AboutFileImageName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AboutText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AboutFileImageName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AboutText)
        </td>
        <td>
            
        </td>
    </tr>
}

</table>
